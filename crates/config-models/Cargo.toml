[package]
name = "config-models"
version = "0.1.0"
edition = "2024"

[dependencies]
config-core = { path = "../config-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
serde_yaml = "0.9"
